import React from 'react';
import ReactDOM from 'react-dom';
import PropertyPlatform from './App';

ReactDOM.render(
  <React.StrictMode>
    <PropertyPlatform />
  </React.StrictMode>,
  document.getElementById('root')
);